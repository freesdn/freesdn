# Operator-supplied keys / certs / credentials must never be committed.
# This .gitignore keeps the directory in version control (via .gitkeep
# + README.md) but excludes every file the operator might drop here.
*
!.gitignore
!.gitkeep
!README.md
